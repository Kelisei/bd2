# La URL va a localhost porque estás testeando desde tu compu hacia el Docker
spring.datasource.url=jdbc:mysql://localhost:3306/bd2_tours?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# ACÁ ESTÁ LA MAGIA:
spring.datasource.password=rootpassword

# Esto le dice a Hibernate que se porte bien con MySQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
